name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff
      - name: Lint
        run: ruff check src/ tests/

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package with dev deps
        run: pip install -e ".[dev]"
      - name: Run tests with coverage
        run: pytest --cov=peanut_agent --cov-report=xml --cov-report=term
      - name: Upload coverage
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml

  security-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify no shell=True in source
        run: |
          # Search only .py files, exclude comments and docstrings
          MATCHES=$(grep -rn --include='*.py' 'shell=True' src/ | grep -vE '^\s*#|"""' || true)
          if [ -n "$MATCHES" ]; then
            echo "$MATCHES"
            echo "FAIL: shell=True found in source code"
            exit 1
          fi
          echo "PASS: No shell=True in source"
